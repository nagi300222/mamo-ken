// マモ拳（仮） VIS-R1.1 (1): 選択アイコン(facesq_{id}.png)手動クロップテーブル
//
// tools/face_metrics.mjsの自動顔検出は、assets/portraits/{id}.png(フィギュア調の
// 立体素材)で拳・爪の暗色を鼻として誤検出することがあり、9体すべてで安定した
// クロップが作れなかった。そこで発注者が実機スクリーンショットで検収した
// 鼻位置・クロップ半径を、素材更新時にも同じ切り出しが再現できるよう
// このテーブルに固定値として保持する(facesq生成の一次ソースはこちら。
// 自動検出はテーブルに無いキャラが増えた場合のみのフォールバックに降格する)。
//
// 各エントリ: [中心x比(画像幅に対する比率), 中心y比(画像高に対する比率), 半径/高さ比]
// クロップ矩形は正方形(比率不変)。side = 2 * r * 画像高。中心は(cx*幅, cy*高)。
export const FACESQ_MANUAL_TABLE = {
  moguzo: { cx: 0.619, cy: 0.159, r: 0.221 },
  pisuke: { cx: 0.476, cy: 0.111, r: 0.221 },
  godan: { cx: 0.52, cy: 0.105, r: 0.235 },
  hakuma: { cx: 0.395, cy: 0.144, r: 0.221 },
  chirka: { cx: 0.365, cy: 0.203, r: 0.221 },
  takimaru: { cx: 0.504, cy: 0.142, r: 0.221 },
  yomikage: { cx: 0.55, cy: 0.17, r: 0.18 },
  bullet: { cx: 0.68, cy: 0.101, r: 0.221 },
  dark_moguzo: { cx: 0.619, cy: 0.159, r: 0.221 },
};
