# PRESENTATION-R1 — Completion Report

Final report for the PRESENTATION-R1 pipeline (Issue #79): connect vNext's
already-live combat systems (Ability state machines, all 63 Command moves,
Modern/Classic Cancel) to player-facing presentation that had fallen behind
— HUD/gauge/VFX, わざ表 (move list), the character-detail screen, and
per-character recommended combos — plus a final audit of any remaining
未結線/TODO markers. No combat-logic value was invented or changed anywhere
in this pipeline; every number shown to players was read from, or measured
against, the live engine.

## 1. PRs merged

| PR | Scope | Merge SHA |
|----|-------|-----------|
| #80 | (E) 63-move Command motion runtime audit + Godan cmdStance fix | `11db0505d49ecbe3a09df984d66638a0cef6bd9a` |
| #81 | (A) Ability UI/Gauge/VFX live-connected to HUD | `8f334598eaa998d22301cf2cc2aff45de7766cbe` |
| #82 | (B) Tabbed COMMAND/MODERN/ABILITY わざ表 from live data | `1121b575af32de1e9c36cfd36f79b9bd799dbbd8` |
| #83 | (C+G) Character-detail screen synced to live values | `9e8147b0bb3bd3008e8ac2e5a89a3935f3aaa76d` |
| #84 | (D) Engine-verified combo routes (3/character) + COMBO tab | `0b4d9faf378791a2df9e5df0d801d8591e7b6fb3` |
| #85 | (F) Stale/未結線 audit — held insufficient-spec items, fixed stale comment | `2f3c333a6ccaa30ba18e4c77e30dcea43a37137c` |

All 6 PRs touch only `prototype/mamoken_prototype_v01.html`, `dist/
mamoken_mobile.html`, `test/character-detail-panel.test.mjs` (one PR, updated
assertions only), and this pipeline's own report/doc files. No combat-logic
function (`fightStep()`/`advance()`/`resolveHits()`/`canChain()`/Classic
Cancel gating/any Ability state machine) was ever modified — every PR's own
report states this explicitly and it is re-confirmed by this report's final
regression (§4).

## 2. 63-move Command motion audit (PR #80)

Full 63/63 PASS table: `reports/presentation/COMMAND_MOTION_AUDIT.md`
(generated by `tools/audit_command_motion_runtime.mjs`, which remains in
the repo as a reusable tool). Every Command move for all 9 characters
resolves its own dedicated `cmd_01`..`cmd_07` motion — none silently
fall back to Normal motion — verified through the real
`currentArtRequest()`/`CURRENT_ART_RUNTIME.resolveFrame()` runtime path,
not asserted from static data. Also includes a stance-type frame-index
progression check (regression guard for a since-fixed `stanceF` bug) and
a 25/25 PASS Modern entry-path parity spot-check that exercises Takimaru's
`pendingModernGrab`/`tickPendingModernGrab()` flow directly rather than
just matching move names.

## 3. Presentation work delivered

- **Ability UI/Gauge/VFX (PR #81)**: all 9 characters' Ability state
  (`gutsActive`, `gritAvailable`, `armorStock`, `ironWallReady`, `charge`,
  `darkChainUses`, etc.) now drives a persistent HUD gauge/stock icon and a
  status icon/VFX, reading `MAMOKEN_ABILITY_UI_MANIFEST`'s 47 assets (all
  confirmed present on disk). Presentation-only — read-only against `f.*`.
  See `reports/presentation/ABILITY_UI_VFX_CONNECTION.md`.
- **わざ表 COMMAND/MODERN/ABILITY/COMBO tabs (PRs #82, #84)**: replaced the
  stale, character-agnostic move list with 4 tabs reading live
  `BAL.CMD.moves`/`MODERN_PATTERNS`/`VERIFIED_COMBO_ROUTES` per character,
  with player-facing badges (`moveBadges()`) derived from named fields
  (cost, armor windows, unblockable, etc.) rather than raw internal tags.
  See `reports/presentation/MOVELIST_UI_UPDATE.md`.
- **Character-detail screen sync (PR #83)**: performance card now shows
  live `r1Char()`/`normalChainLimitFor()` values instead of 1-5 relative
  catalog numbers; move implementation-status cross-references
  `BAL.CMD.moves` at render time instead of trusting the stale
  `character-catalog.ts` `implementationStatus` field (which predates
  CMD-R1/vNext R2-R4 and was intentionally left as historical/design-
  reference data rather than edited directly — see rationale in
  `reports/presentation/CHARACTER_DETAIL_SYNC.md`). Also added a Classic
  Cancel explainer card and fixed a Codex-flagged Yomikage timing
  description error.
- **Engine-verified combo routes (PR #84)**: replaced the catalog's
  `UNVERIFIED_COMBOS` placeholder ("コンボは全枠 未検証") and the old
  shared 4-route `COMBO_ROUTES` with 27 routes (3 per character), each
  **actually executed** in the live engine via the same single-choke-point
  functions (`startAtk`/`startCmdAtk`/`startCmdGrab`) every real input path
  uses, with damage read from measured `hp` deltas — never from static
  move data. All 27 are confirmed TRUE COMBO in the sense the harness
  checks (opponent never `idle`/`guard`-actionable between hits),
  validated by a deliberate adversarial control that proves the harness
  correctly reports a whiff rather than rubber-stamping success. Full
  methodology and table: `reports/balance/
  VNEXT_PRESENTATION_COMBO_ROUTES.md`.
  **Caveat confirmed during this report's own review (Codex, PR #86)**:
  `applyInputs()` lets Roar interrupt `hitstun` unconditionally once
  `f.s>=BAL.SMAX` (no frame-window restriction, unlike the `idle`/`guard`
  actionable check the harness samples) — re-running the Route C sequence
  for all 9 characters with the defender's `s` pre-set to `BAL.SMAX`
  confirmed every route has at least one such Roar-escapable gap. This
  does not change any measured damage number (Roar never fires in the
  original verification, since the passive dummy defender starts and
  stays at `s:0`), but "TRUE COMBO" here specifically means "unescapable
  via block/dodge/normal reversal," not "unescapable under all
  circumstances including a maxed S gauge" — the same caveat any combo
  in this engine carries once the defender's meter is full. Tracked as a
  documented limitation rather than a defect, since Roar-as-combo-breaker
  is intentional combat-logic design (untouched by this presentation-only
  pipeline) and re-classifying or renaming the label would itself be an
  unrequested combat-logic-adjacent judgment call.

### Measured combo damage (all 27 routes, TRUE COMBO*)

\* see the Roar-escape caveat above — true against block/dodge/normal
reversal; a defender with a maxed S gauge can Roar out of the hitstun gap
between any two hits in any of these 27 routes.

| Character | A | B | C |
|---|---|---|---|
| Moguzo | 中段→上段: 146 | 下段→中段→上段: 262 | 地走り→昇撃: 197 |
| Pisuke | 中段→上段: 113 | 下段→中段→上段→下段: 268 | 二連牙→宙返り蹴: 151 |
| Godan | 中段→上段: 200 | 上段→下段: 289 | 岩砕き→天蓋落とし: 302 |
| Hakuma | 中段→上段: 137 | 下段→中段: 181 | 雪壁掌→氷柱返し: 166 |
| Chirka | 中段→上段: 133 | 下段→中段→上段: 237 | だまし突き→戻り蹴り: 174 |
| Takimaru | 中段→上段: 162 | 下段→中段: 213 | ぶちかまし→熊手払い: 198 |
| Yomikage | 中段→上段: 118 | 下段→中段: 158 | 影縫い→月かすめ: 141 |
| Bullet | 中段→上段: 146 | 下段→中段→上段: 259 | 弾み突き→尾払い: 147 |
| Dark Moguzo | 中段→上段: 123 | 下段→中段→上段→下段: 289 | 黒走り→逆昇撃: 145 |

## 4. Stale/未結線 audit results (PR #85)

Swept `prototype/mamoken_prototype_v01.html` for `(未結線)`-tagged fields
(internal-only — confirmed via grep that `moveBadges()`/every render path
reads named fields, never the raw `tags` array, so none of this text has
ever reached a player) and for other `TODO`/`design_confirmed` markers
across `prototype/`, `docs/`, and `design/`.

- **Implemented**: fixed the `hardDown` inline comment, which claimed no
  move used it — stale since Godan's 天蓋落とし/弾丸頭突き both do.
- **Held as insufficient-spec** (documented in full, with reasoning, in
  `docs/implementation/vnext/CLOSURE_STATUS.md`'s "Held due to
  insufficient spec" section — not implemented, per the standing rule
  against guessing undecided values):
  - Moguzo 胴押し, Hakuma 不動押し — tagged "small Pushback"/"small push"
    in `COMBAT_VNEXT_FINAL.md` §13, but no doc or code anywhere gives an
    exact numeric magnitude, and no named knockback-tier constant exists
    in the engine to infer one from.
  - Pisuke すり抜け足, つむじ返し — exact `ENTER_CLINCH`/
    `CHASE_TO_CONTACT`+`maximumApproachSteps:2` values exist in
    `design/combat/contracts/MAMOKEN_CURRENT_3_CHARACTERS_MOVESPEC_
    CLOSURE_v0.2.json`, but implementing them requires a spatial/movement
    subsystem (CLINCH posture, opponent-approach stepping) the live
    engine has zero architecture for — combat resolution here is purely
    phase/state-machine driven, with no position/distance gating
    anywhere. Disproportionate scope for a presentation-only pass.
  - Godan 大山押し flagged for the same "small push"-with-no-magnitude
    reason, for consistency, even though it carried no `(未結線)` tag.
- **Nothing else found**: broader sweep of `docs/`/`design/` for other
  markers turned up nothing actionable beyond what's listed above.

## 5. Final regression (this report)

Re-ran vNext PR5's own two proofs against the final PRESENTATION-R1 state
(`main` @ `2f3c333`, PR #85 merged) to confirm none of PRs #80-#85 — all
presentation-only — regressed what PR5 already established:

- **72 ordered-pair offline crash sweep**: all 9 characters as P1 against
  all 8 others and again as P2 against all 8 others (both seatings are
  distinct code paths — P1 routes through `evq`/`currentP1Hold()`, P2
  through `cpuEv`/AI), run to natural round completion via the real
  `fightStep()` — **0 exceptions, 0 console errors, 72/72 pairs clean**.
- **Full-state determinism hash (repeatability)**: 3 matchups, same fixed
  `mulberry32` seed (bypassing `startBattle()`'s wall-clock seed, same
  technique PR5 used), 2 independent runs each on the final state, per-
  tick FNV-1a hash of the entire `B` object minus `fx` (the one field PR5
  already proved diverges only via decorative unseeded `Math.random()`
  particle variance, never gameplay logic) — byte-identical at every
  tick, all 3 matchups, both runs.
- **Full-state determinism hash vs. the pre-pipeline baseline (actual
  non-regression proof)**: a first pass of this report only compared the
  final state against itself, which proves repeatability but — as
  correctly flagged by a Codex review on this PR (PR #86) — cannot by
  itself rule out a regression that is *itself* deterministic (identical
  bug on both final-state runs). Fixed by actually running the same 3
  matchups/seed/tick-count against `prototype/mamoken_prototype_v01.html`
  checked out at `b8575cd` (the vNext PR5 closure merge commit,
  immediately pre-PRESENTATION-R1, via `git worktree`) and diffing its
  per-tick hash sequence against the final state's (`main` @ `2f3c333`)
  — **byte-identical at every tick, same sequence length, all 3
  matchups, zero page errors on either side**. This is the direct
  comparison Codex asked for, not an inference from "no combat-logic
  file was touched"; it now genuinely proves PRs #80-#85 introduced zero
  gameplay-state divergence from the PR5 baseline.
- **`npm run build:mobile` reproducibility**: 3 independent back-to-back
  builds produce MD5-identical `dist/mamoken_mobile.html`
  (`9c6ecaf195fd4f2c4d4a8ab5b3d25df8`).
- **All 35 `npm run check:*` scripts**: green on `main` post-merge.

This confirms the presentation-only claim every individual PR made is
still true in aggregate: the full gameplay-relevant state machine and its
determinism are unchanged from vNext PR5's own baseline — now backed by
a direct baseline comparison rather than argued from omission.

## 6. Hash-invariance proofs (per-PR, already reported)

- PR #81: 5 matchups × 4000 ticks, pre- vs post-Ability-UI-connection,
  byte-identical full-state hash — confirms the new HUD rendering reads
  `f.*` only, never writes it.
- PR #84: the adversarial control script (§1 of `VNEXT_PRESENTATION_
  COMBO_ROUTES.md`) confirms the combo-verification harness itself
  correctly reports a whiff rather than a false success.
- This report's §5: full-state hash re-confirmed at pipeline close.

## 7. Remaining/deferred items

All held-due-to-insufficient-spec items are listed in §4 above and in
`docs/implementation/vnext/CLOSURE_STATUS.md`'s "Held due to insufficient
spec (PRESENTATION-R1 (F))" section. No other implementation work remains
open from this pipeline's scope.

## 8. Items only verifiable on real hardware / two-device setup

Carried forward, unchanged, from vNext PR5's own "Known gap" section
(`CLOSURE_STATUS.md`): **online regression** (`onlineStep()`/
`netFightStep()`, slot-dependent queue mapping, real peer message
ordering/latency, disconnect handling) was never exercised end-to-end in
this sandbox, because the local Cloudflare Workers relay could not be
stood up under this environment's network restrictions. PRESENTATION-R1
touched no online-relevant code path (all 6 PRs are HUD/movelist/
character-detail/combo-data changes gated behind offline/pre-battle
screens or presentation-only render functions), so this gap is unchanged
in scope from PR5, not newly introduced — but it remains open and should
be run by whoever has a real 2-device or deployed-relay environment
before treating any of vNext or PRESENTATION-R1 as fully closed
end-to-end.
