name: T24 generate extended command shadow

on:
  push:
    branches: [feature/extended-command-shadow-v1]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    if: ${{ github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/extended-command-shadow-v1
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
      - run: npm ci
      - name: Clean temporary test scaffold
        run: |
          python - <<'PY'
          from pathlib import Path
          path=Path('test/runtime-extended-command-shadow.test.mjs')
          text=path.read_text()
          text=text.replace("const catalogApi=vm.runInNewContext('api', {api:load().constructor ? undefined : undefined});\nvoid catalogApi;\n",'')
          path.write_text(text)
          PY
      - name: Apply runtime hook and build integration
        run: node tools/apply_t24_extended_command_shadow.mjs
      - name: Build mobile distribution
        run: npm run build:mobile
      - name: Run extended shadow tests
        run: npm run check:runtime-extended-shadow
      - name: Commit generated changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add prototype/mamoken_prototype_v01.html tools/build_mobile.mjs dist/mamoken_mobile.html test/runtime-extended-command-shadow.test.mjs
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "[t24] integrate extended command shadow into runtime build"
          git push origin HEAD:feature/extended-command-shadow-v1
