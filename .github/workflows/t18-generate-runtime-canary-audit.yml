name: T18 generate runtime canary audit

on:
  push:
    branches:
      - 'feature/runtime-canary-audit-v1'

permissions:
  contents: write

concurrency:
  group: t18-runtime-canary-audit
  cancel-in-progress: false

jobs:
  generate:
    if: ${{ !contains(github.event.head_commit.message, '[t18-generated]') }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout feature branch
        uses: actions/checkout@v4
        with:
          ref: feature/runtime-canary-audit-v1
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Apply runtime canary audit hook
        run: node tools/apply_t18_runtime_canary_audit.mjs

      - name: Build mobile distribution
        run: npm run build:mobile

      - name: Commit generated runtime files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add prototype/mamoken_prototype_v01.html dist/mamoken_mobile.html
          if git diff --cached --quiet; then
            echo "No generated changes"
          else
            git commit -m "[t18-generated] apply deterministic canary audit"
            git push origin HEAD:feature/runtime-canary-audit-v1
          fi
